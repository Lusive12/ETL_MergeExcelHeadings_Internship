ENTITY, DIVISION, AREA & FUNCTION
==================================
SOURCE FILES (place here before running):
  [source files for entity/division/area/function lookup]

OUTPUT (produced by the automation):
  IKP_PQAH_With_Entity_DivisionDesc_Area_Function.xlsx
    -> Used by the Final Assembler:
       col Y  = Entity
       col Z  = Division
       col AA = Area
       col AB = Function

Note: Based on IKP_PQAH.xlsx as the primary source,
      enriched with organizational hierarchy data.